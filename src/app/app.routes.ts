import { Routes } from '@angular/router';
import {LandingCompleteComponent} from './public/pages/landing-complete/landing-complete.component';

export const routes: Routes = [
  {path:'home',component:LandingCompleteComponent}
];
